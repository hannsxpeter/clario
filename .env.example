# Convex deployment URL. Written automatically by `npx convex dev`.
PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# The DeepSeek API key lives in Convex (server side), not in this file.
# Set it once with:
#   npx convex env set DEEPSEEK_API_KEY <your-key>
# For production:
#   npx convex env set --prod DEEPSEEK_API_KEY <your-key>
